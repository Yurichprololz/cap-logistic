const constansts = Object.freeze({
    errors: {
        busyDriver: 'The driver has already bound to car',
        invalidName: 'The name and surname should contain only letter',
    }
})
module.exports = constansts